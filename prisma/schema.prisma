datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        Int       @id @default(autoincrement())
  email     String    @unique
  fullName  String
  field     String
  bio       String?
  createdAt DateTime  @default(now())
  episodes  Episode[]
  seasons   Season[]
}

model Season {
  id          Int       @id @default(autoincrement())
  seasonNum   String
  titleFa     String
  titleEn     String
  isCompleted Boolean   @default(false)
  createdAt   DateTime  @default(now())
  userId      Int
  user        User      @relation(fields: [userId], references: [id])
  episodes    Episode[]

  @@unique([userId, seasonNum])
}

model Episode {
  id          Int      @id @default(autoincrement())
  titleFa     String
  titleEn     String
  descFa      String
  descEn      String
  audioUrl    String
  coverUrl    String
  duration    String
  episodeNum  String
  status      String   @default("published")
  transcript  String?
  aiAnalysis  String?
  createdAt   DateTime @default(now())
  userId      Int?
  user        User?    @relation(fields: [userId], references: [id])
  seasonId    Int?
  season      Season?  @relation(fields: [seasonId], references: [id])
  reports     Report[]

  @@unique([seasonId, episodeNum])
}

model Report {
  id        Int      @id @default(autoincrement())
  ipAddress String
  reason    String
  createdAt DateTime @default(now())
  episodeId Int
  episode   Episode  @relation(fields: [episodeId], references: [id])

  @@unique([ipAddress, episodeId])
}

model AiHistory {
  id            Int      @id @default(autoincrement())
  fileName      String
  audioUrl      String?
  transcription String
  analysis      String?
  summaryFa     String?
  summaryEn     String?
  createdAt     DateTime @default(now())
}
